{% extends 'base.html.twig' %}

{% block title %}
  Quiz
{% endblock %}

{% block body %}
  <style>
    .form-check-label { margin-left: 30px; }
  </style>

  {% include 'topBar/topBar.html.twig' %}
  <div class="container">
    <form id="test_submit"
      role="form"
      action="{{ path('result_test') }}"
      method="post">
      {% for question in questions %}
        <div class="panel panel-default">
          <div class="panel-heading">
            Question N°{{ question.id }} {{ question.title }}
          </div>
          <div class="panel-body">
            {% for rep in question.reponses %}
              <div class="form-check">
                <input class="form-check-input"
                  type="radio"
                  name="{{ rep.rep }}"
                  value="{{ rep.status }}"
                  checked />
                <label class="form-check-label" for="exampleRadios1">
                  {{ rep.rep }}
                </label>
              </div>
            {% endfor %}
          </div>
        </div>
      {% endfor %}
    </form>
    <button class="btn btn-success" id="test" value="Submit">Valider QCM</button>

    <br />
  </div>

  <script>

  </script>
{% endblock %}
